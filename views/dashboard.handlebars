<!DOCTYPE html>
<html>
<head>
    <title>User Dashboard - TechQikBlog</title>
    <!-- Add CSS and Dependencies -->
</head>
<body>
    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/dashboard">Dashboard</a></li>
            <li><a href="/logout">Logout</a></li>
        </ul>
    </nav>

    <h1>User Dashboard</h1>

    <h2>Welcome, {{user.username}}!</h2>

    <h3>Your Blog Posts:</h3>
    {{#if user.posts}}
        <ul>
            {{#each user.posts}}
                <li>{{this.title}}</li>
            {{/each}}
        </ul>
    {{else}}
        <p>No blog posts yet.</p>
    {{/if}}
</body>
</html>